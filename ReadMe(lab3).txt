I believe the time complexity for this algorithm will be O(n^2) or 4n^2 due to the for for loop I use along with a nested for loop that goes through i n times in line 13
The space complexity would be 1 due to there being a set array, that contains strings.
